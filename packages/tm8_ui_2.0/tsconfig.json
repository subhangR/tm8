{
  "//": "Mirrors packages/ui/tsconfig.json (the gate-verified browser config) minus the react-mentions pin this package does not need. Tests are compiled and run by Vitest, not tsc — test-only imports of node builtins (node:fs for the tokens byte-equality test) must not leak into the browser build, and app code stays guarded against accidental node imports because `types` omits @types/node.",
  "compilerOptions": {
    "target": "ES2021",
    "useDefineForClassFields": true,
    "lib": ["ES2021", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "Bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "types": ["vite/client"]
  },
  "include": ["src"],
  "exclude": [
    "src/**/*.test.ts",
    "src/**/*.test.tsx",
    "src/**/*.spec.ts",
    "src/**/*.spec.tsx"
  ]
}
