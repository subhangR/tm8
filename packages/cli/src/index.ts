#!/usr/bin/env node
// @tm8/cli — graph command tree + compat adapter (R18 frozen list) + manifest
// reader (worker init). Placeholder — real work starts at W4 (M3).

export const CLI_PACKAGE = "@tm8/cli";
