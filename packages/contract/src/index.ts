// @tm8/contract — THE LAW.
// Types + zod schemas + operation catalog + WorkspaceEvent + error taxonomy.
// Authored at M0 from docs/COLLAB_V2_UI_DATA_CONTRACT.md (as implemented by the
// collab-v2 UI's types/) + docs/collab-v2-api-design/, extended with the
// execution.* family (R16) and work_session/collection/custom-kind shapes.
// Placeholder module — populated during W0.

export const CONTRACT_VERSION = "0.1.0";
