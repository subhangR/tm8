/**
 * home-page — the merged single home (task 01a0027d): chat hero + NEEDS YOU
 * strip + glance rails + presence row. Mounted by GateApp at view ref
 * `dashboard`, which is the landing target.
 */
export { HomePage, type HomePageProps } from './HomePage';
