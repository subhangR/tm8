# SURFACE WAVE — the complete-everything program order (2026-07-29)

**status-as-of:** `6dce6da` · Author: fe-coordinator · Bar: LINK-LEVEL COMPLETENESS — every drawn control exists and either works or renders disabled-with-reason; zero silent voids. Fidelity explicitly deferred to a later parity session (user order). ATELIER tokens + honesty laws still bind; no new debt, no polish.

All workers: opus-5, quiet protocol (task reports + handover file in own lane, never session prompts), fires-red-first tests, wide check from packages/tm8-ui, no git ever, coordinator wires mounts and commits. Laws via DETAIL-SCREENS-WORKER-BRIEF.md; scope via each spawn message.

| # | lane | canvas | owns (exclusively) | scope one-liner |
|---|------|--------|--------------------|-----------------|
| 1 | home | T5-1 Home Dashboard | `src/home/` | The Dashboard route's screen: anatomy frame, 320 floor, dark. |
| 2 | settings-space | T2 (frames: space, members, invites, menu editor) | `src/settings-space/` | Space settings suite half A. |
| 3 | settings-governance | T2 (frames: projects/trust, interaction profiles, custom kinds) | `src/settings-governance/` | Governance half B — trust UI ranked first inside the lane. |
| 4 | inbox | T3 Files, Node & Inbox (inbox frames) | `src/inbox/` | Inbox screen + read-state honesty (mark-read verb is vocabulary now). |
| 5 | files-node | T3 Files, Node & Inbox (files + node room frames) | `src/files/` | Files screen + node room. |
| 6 | account-menu | T3-3 Account Menu | `src/account/` | The real account menu (theme's ruled home; avatar label reverts on mount). |
| 7 | doc-edit | T5-3 Doc Authoring | `src/doc-edit/` | Doc authoring/edit surface over patchEntity. |
| 8 | discussion | T5-7 Discussion Tab Body | `src/discussion/` | The full discussion body (threads, mentions, embeds); composer seam-post already wired — extend, don't rebuild. |
| 9 | channel-screen | T10 Chat Surface | `src/channel-screen/` | The channel chat surface behind the hub's Feed. |
| 10 | layouts | T5-2 Board/Feed/Gallery | `src/layouts/` | The three alternate list layouts + states/floors/dark frame; EntityView's view-option toggles consume them later. |
| 11 | kind-bodies-2 | T0-4 (governed/restricted/generic-variant frames) | `src/panels/bodies/` NEW FILES ONLY: `GovernedBody.tsx/.test.tsx/governed-body.css`, `RestrictedBody.tsx/.test.tsx/restricted-body.css` | project (governed, TRUST SAFETY UI first) + interaction_profile (restricted); richer generic variants (file/spell/skill/PR/commit/message) as registry BLOCK specs in the handover, not code, if time-boxed. `collection` has NO canvas — composed-from-patterns, FLAGGED as such in its handover, coordinator ratifies before wiring. |

NOT A WORKER: facet pixel supply for the six undrawn delivery states — a DESIGN input, flagged back to master.

Coordinator's own parallel backlog (my hands, between handovers): real React error boundary; in-sheet spawn refusal card; auth + authoring mounts on their handovers; void routes dispatch to these surfaces as each lands; ledger sweep D64+.
