node_modules
dist/
*.tsbuildinfo
.DS_Store
.env
.env.local
.env.*.local
target/
coverage/
test-results/
playwright-report/
dist-base/
dist-mine/
# The alternate UI bundles, from the days when a second UI package was served
# beside the product one. Both packages were deleted on 2026-09-06; these stay
# listed because a checkout that predates the deletion still has the built
# directories on disk and they must not turn up as untracked noise.
dist-1.0/
dist-2.0/
