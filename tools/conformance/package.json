{
  "name": "@tm8/conformance",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc -b",
    "generate": "bun src/generate.ts",
    "check:generated": "bun src/generate.ts --check",
    "test": "bun run check:generated && vitest run --config vitest.config.ts",
    "test:foundation": "vitest run --config vitest.config.ts",
    "test:live": "vitest run --config vitest.live.config.ts",
    "stub": "bun src/stub-main.ts"
  },
  "dependencies": {
    "@tm8/contract": "workspace:*",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "@types/node": "^20.14.0"
  }
}
